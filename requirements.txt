# Runtime dependencies for the hosted deployment.
#
# The engine itself has none: afc.py / afc2.py / afc5.py / presets.py and the
# C++ core in afc_native.cpp use only the standard library and ctypes.  What
# is listed here serves the web layer, the PostgreSQL backend and the object
# store.  Pinned so a redeploy during the study period cannot change the
# software under test.
Flask==3.1.3
Werkzeug==3.1.8
python-dotenv==1.2.3

# PostgreSQL backend (db_pg.py).  The [binary] extra ships libpq in the wheel,
# so the host needs no PostgreSQL client packages installed.
psycopg[binary]==3.3.4

# Supabase Storage backend (storage_supabase.py).
supabase==2.31.0

# WSGI server.  Not imported by the application; see Procfile.
gunicorn==26.2.0
